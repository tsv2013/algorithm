language: node_js

before_script:
  - npm install -g grunt
  - npm install -g grunt-cli
  - npm install -g bower
  - npm install -g typescript
  - npm install -g tsd
  - tsd install knockout
  - tsd install jquery
  - tsd install qunit
  - bower install
 
script: grunt test
